# AGENTS

Fleet conventions apply to this repo.

Poteto mode is on. Verify against the real artifact. Use small verifiable units. Commit each unit before the next.

Use conventional commits. Keep diffs small. Prefer stdlib and installed deps. Delete speculative code.

Never write local.properties. SDK comes from ANDROID_HOME. Java toolchain is 17.
